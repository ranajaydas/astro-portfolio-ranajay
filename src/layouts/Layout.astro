---
import "@assets/styles/base.css";

export interface Props {
	title?: string;
}

const { title } = Astro.props;
console.log("ANALYTICS!!", process.env.PUBLIC_VERCEL_ANALYTICS_ID);
console.log("ANALYTICS 2!!", process.env.VERCEL_ANALYTICS_ID);
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta
			name="description"
			content="Hi 👋🏾! I'm Ranajay Das, a Fullstack Developer & Author with 15+ years of software building experience."
		/>
		<meta name="viewport" content="width=device-width" />
		<link
			rel="icon"
			href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👋🏾</text></svg>"
		/>
		<meta name="generator" content={Astro.generator} />
		<title>{`Ranajay Das | ${title || "Fullstack Developer & Author"}`}</title>
		<link
			rel="preload"
			href="/fonts/epilogue-600.woff2"
			as="font"
			type="font/woff2"
			crossorigin
		/>
		<link
			rel="preload"
			href="/fonts/epilogue-regular.woff2"
			as="font"
			type="font/woff2"
			crossorigin
		/>
	</head>
	<body>
		<slot />
	</body>
</html>

<script>
	console.log("ANALYTICS!!", process.env.PUBLIC_VERCEL_ANALYTICS_ID);
	console.log("ANALYTICS 2!!", process.env.VERCEL_ANALYTICS_ID);
</script>
